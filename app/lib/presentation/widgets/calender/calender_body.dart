import 'package:flutter/material.dart';
import 'package:geotrackr/presentation/widgets/calender/calender.dart';

/// The [CalenderBody] widget is a stateless widget that represents the body of the calendar page.
/// It displays the calendar widget.
class CalenderBody extends StatelessWidget {
  const CalenderBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Calender();
  }
}
